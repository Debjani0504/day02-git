# Patient table
|Column| Data type|Reason
|Patient_id|INT|ID
|first_name|VARCHAR(50)|Name
|last_name|VARCHAR(50)|name
|date_of_birth|Date|INT|Birth Date|
|Phone|VARCHAR(15)|Phone number|
|email|VARCHAR(100)|email_id|
|is_active|TINYINT(1)|Yes/NO|